;
;	Tablacus DOS cmd
;
	ALIGN	256	;0C000Hに配置
;			下位1バイト0になるように
CPM_BOOT:
	JP	WBOOT1
CPM_WBOOT:
	JP	WBOOT1
CPM_CONST:			;(BDOS)コンソール直接入力
	JP	CONST1
CPM_CONIN:
	JP	CONIN1
CPM_CONOUT:
	JP	CONOUT1

RAM_TABLE:
BASIC_RAM_0	EQU	RAM_TABLE
BASIC_RAM_1	EQU	RAM_TABLE+1
BASIC_RAM_2	EQU	RAM_TABLE+2
COMMON_RAM_3	EQU	RAM_TABLE+3
DOS_RAM_0	EQU	RAM_TABLE+4
DOS_RAM_1	EQU	RAM_TABLE+5
DOS_RAM_2	EQU	RAM_TABLE+6
BACKUP_RAM_3	EQU	RAM_TABLE+7

MMAP_TABLE_PTR	EQU	RAM_TABLE+8
MAPPER_JUMP_TABLE	EQU	MMAP_TABLE_PTR+2
MAPPER_ALL_SEG	EQU	MAPPER_JUMP_TABLE
MAPPER_PUT_PH	EQU	MAPPER_JUMP_TABLE+012H
MAPPER_GET_PH	EQU	MAPPER_JUMP_TABLE+015H
